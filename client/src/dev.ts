/**
 * Frontend Dev Mode Flag
 * 
 * When set to true, enables development mode that allows:
 * - Testing onboarding and UI flows without backend
 * - No database or server required
 * - Fake authentication
 * - No redirects to /api/login
 * 
 * Set to false to restore normal production behavior.
 */
export const DEV_MODE = true;

