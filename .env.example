# Dwellpath Demo Mode
#
# Demo Mode is designed for Netlify/static SPA deployment (no backend required).
# Set to "true" to use in-browser mock data (no /api calls).
# Set to "false" only if you are running the optional Express API locally.
VITE_DEMO_MODE=true

# Optional backend/server variables (only used when running dev:server/dev:full)
DATABASE_URL=
SESSION_SECRET=
OPENAI_API_KEY=
