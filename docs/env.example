# Copy this file to `.env` at the repo root if you want to override defaults.

# Client (Vite)
# Use in-browser mock data instead of calling /api
VITE_DEMO_MODE=true

# Server (optional - only needed if you run `npm run dev:server` or `npm start`)
PORT=5000
DATABASE_URL=
SESSION_SECRET=
OPENAI_API_KEY=

