 Replit Prompt: Modern Sophisticated Iconography for dwellpath (React + Tailwind)
markdown
Copy
Edit
🔧 Create a modern, elegant icon showcase for the **dwellpath** React app using Heroicons (24/outline). These icons should visually reflect:
- Navigation
- Confidence
- Privacy
- Tracking
- Trust
- Compliance

---

## 📦 File: `ElegantIconGrid.jsx`

```jsx
import {
  GlobeAltIcon,       // Global presence
  ArrowTrendingUpIcon, // Financial/legal growth
  FingerPrintIcon,    // Identity, privacy
  ShieldCheckIcon,    // Trust, compliance
  CursorArrowRaysIcon,// AI or insight
  Squares2X2Icon      // Organized structure
} from '@heroicons/react/24/outline';

export default function ElegantIconGrid() {
  const iconClasses = 'h-8 w-8 stroke-[1.5] text-gold dark:text-dark.gold';
  const items = [
    { icon: <GlobeAltIcon className={iconClasses} />, label: 'Global Residency' },
    { icon: <ArrowTrendingUpIcon className={iconClasses} />, label: 'Residency Growth' },
    { icon: <FingerPrintIcon className={iconClasses} />, label: 'Identity & Privacy' },
    { icon: <ShieldCheckIcon className={iconClasses} />, label: 'Compliance & Protection' },
    { icon: <CursorArrowRaysIcon className={iconClasses} />, label: 'AI Insights' },
    { icon: <Squares2X2Icon className={iconClasses} />, label: 'Domicile Structure' },
  ];

  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 bg-sand dark:bg-dark.surface text-navy dark:text-dark.cream p-10 rounded-2xl shadow-lg font-body">
      {items.map(({ icon, label }) => (
        <div key={label} className="flex items-center space-x-4">
          {icon}
          <span className="text-lg">{label}</span>
        </div>
      ))}
    </div>
  );
}
✅ Styling Notes
Use stroke-[1.5] to make icons feel sleek and intentional

Stick with text-gold / text-dark.gold for subtle luxury

Typography: font-body text-lg keeps it clean and clear

💡 Optional Enhancements
Wrap this as <IconSection title="Trust & Tracking" />

Animate with Tailwind's hover:scale-105 and transition-transform

Add a size prop to dynamically control icon size across app

