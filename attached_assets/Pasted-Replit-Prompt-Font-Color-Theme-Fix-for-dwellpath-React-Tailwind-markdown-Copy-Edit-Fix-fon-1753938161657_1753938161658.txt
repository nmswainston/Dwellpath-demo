Replit Prompt: Font + Color Theme Fix for dwellpath (React + Tailwind)
markdown
Copy
Edit
🔧 Fix font color and font-family consistency across the **dwellpath** React app using Tailwind CSS. Ensure:

1. All text elements default to the correct brand fonts
2. Colors switch cleanly between light and dark mode
3. Body text and headings apply globally

---

## ✅ Step 1: Update `tailwind.config.js`

```js
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        navy: '#0A1F44',
        cream: '#EFE6D9',
        slate: '#A5ACB8',
        sand: '#F6F2EC',
        gold: '#D4B074',
        dark: {
          background: '#0A1A2F',
          surface: '#1F2C45',
          cream: '#F9F5EF',
          gold: '#D2AD7E',
          slate: '#94A3B8',
        },
      },
      fontFamily: {
        heading: ['"Playfair Display"', 'serif'],
        body: ['Inter', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
✅ Step 2: Import Fonts in index.html
html
Copy
Edit
<!-- public/index.html -->
<link href="https://fonts.googleapis.com/css2?family=Inter&family=Playfair+Display&display=swap" rel="stylesheet">
✅ Step 3: Add Global Styles in index.css or tailwind.css
css
Copy
Edit
@tailwind base;
@tailwind components;
@tailwind utilities;

html {
  font-family: 'Inter', sans-serif;
  background-color: theme('colors.sand'); /* fallback if needed */
  color: theme('colors.navy');
}

body {
  @apply bg-sand dark:bg-dark.background text-navy dark:text-dark.cream font-body transition-colors duration-300;
}
✅ Step 4: Apply Fonts Consistently in JSX
🔤 Headings
Use:

jsx
Copy
Edit
<h1 className="font-heading text-4xl">Heading</h1>
📄 Body Text
Use:

jsx
Copy
Edit
<p className="font-body text-base">Paragraph content</p>
You can also add font-body to layout wrappers or utility components to enforce fallback consistency.

🧠 Optional: Add base Styles via Tailwind Plugin
For global type styling:

js
Copy
Edit
// tailwind.config.js
plugins: [
  require('@tailwindcss/typography'),
],
Then wrap content in prose classes.